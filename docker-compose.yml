services:
  app:
    image: athenas:x86
    container_name: athenas-dev
    ports:
      - "5002"
    networks:
      - app_network
    volumes:
      - .:/src
    working_dir: /src
    env_file:
      - .env
    command: ["gunicorn", "-w", "4", "-b", "0.0.0.0:5002", "app:create_app()"]

networks:
  app_network:
    driver: bridge